import { locationPopularity } from 'craigslist/helpers/location-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | location popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = locationPopularity([42]);
  assert.ok(result);
});
