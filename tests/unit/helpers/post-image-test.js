import { postImage } from 'craigslist/helpers/post-image';
import { module, test } from 'qunit';

module('Unit | Helper | post image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postImage([42]);
  assert.ok(result);
});
